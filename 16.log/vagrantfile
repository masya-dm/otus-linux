Vagrant.configure(2) do |config|
	config.vm.box = "centos/7"
	config.vm.hostname = "log"
	config.vm.network "private_network", "ip: 192.168.11.151"
	config.vm.define "log"
	config.vm.provider :virtualbox do |vb|
            vb.customize ["modifyvm", log, "--memory", "256"]
          end
        log.vm.provision "shell", inline: <<-SHELL
            mkdir -p ~root/.ssh; cp ~vagrant/.ssh/auth* ~root/.ssh
            sed -i '65s/PasswordAuthentication no/PasswordAuthentication yes/g' /etc/ssh/sshd_config
            systemctl restart sshd
            systemctl stop firewalld
            setenforce 0
            yes | cp -rf /usr/share/zoneinfo/Europe/Moscow /etc/localtime
            yum install -y nano rsyslog
            yes | cp -f /vagrant/conf/server/rsyslog.conf /etc/
            systemctl restart rsyslog.service
          SHELL
     end
